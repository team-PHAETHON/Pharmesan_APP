import 'package:flutter/widgets.dart';

class SearchResultCatalog extends StatelessWidget {
  const SearchResultCatalog({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      
    );
  }
}